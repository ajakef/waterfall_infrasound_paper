Setup: create and activate an environment with the correct dependencies with
```
conda env create -f environment.yml
conda activate waterfall
```
Then, cd to `code/` and run the scripts there.

Only data required to generate figures is stored in `data/`. The rest can be found at https://scholarworks.boisestate.edu/fluvial_data/.
